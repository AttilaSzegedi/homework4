apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: hw4prod
  labels:
    app: frontapp
spec:
  rules:
  
  - http:
      paths:
      - path: /frontapp
        host: application.cubix.localhost
        pathType: Prefix
        backend: 
          service:
            name: frontapp
            port:
              number: 8080
