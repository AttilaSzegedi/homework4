apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: hw4frontapp
  labels:
    app: frontapp
    homework: frontapp
spec:
  rules:
  - http:
      paths:
      - path: /frontapp
        host: other.cubix.localhost
        pathType: Prefix
        backend: 
          service:
            name: frontapp
            port:
              number: 8080
