apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: hw4frontapp
  labels:
    app: frontapp
    homework: frontapp
spec:
  rules:
  - host: other.cubix.localhost
  - http:
      paths:
      - path: /frontapp
        pathType: Prefix
        backend: 
          service:
            name: frontapp
            port:
              number: 8080
